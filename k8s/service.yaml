apiVersion: v1
kind: Service
metadata:
  name: pdf-translator-service
  namespace: pdf-translator
  labels:
    app: pdf-translator
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: pdf-translator
